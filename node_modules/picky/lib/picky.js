
/*!
 * Picky
 * @license MIT
 */

/*
 * Module dependencies
 */

var path = require('path');
var support = require('./support');

/*
 * Picky
 */

exports.assert = require('./assert');
exports.feature = require('./feature');
exports.Suite = require('./suite');
exports.gulp = createTask('gulp');
exports.grunt = createTask('grunt');

/*
 * createTask
 */

function createTask (name) {
  return function (options) {
    var dep = support(name);
    var task = require(path.join(__dirname, '..', 'tasks', name));
    return task(dep, options);
  }
}
